# backend_flask_heatmap

Crear entorno virtual usando venv para y posteriormente ejecutar pip install -r requirements.txt para instalar las dependencias.

luego ejecutar python app.py para correr el backend
